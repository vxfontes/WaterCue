<Window x:Class="WaterCueWindows.Views.StatsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:oxy="http://oxyplot.org/wpf"
        Title="WaterCue"
        Width="620"
        Height="560"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource WindowBackgroundBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Style="{StaticResource CardBorderStyle}"
                Background="{StaticResource HeroPanelBrush}"
                Padding="22,20"
                Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Estatísticas"
                           Style="{StaticResource WindowTitleTextStyle}" />
                <TextBlock Text="Visão rápida da sua rotina de hidratação."
                           Style="{StaticResource BodyTextStyle}"
                           Margin="0,8,0,0" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="1"
              Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="14" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Border x:Name="TodayCard"
                    Grid.Column="0"
                    Style="{StaticResource CardBorderStyle}"
                    Padding="18,16">
                <StackPanel>
                    <TextBlock Text="Hoje"
                               Style="{StaticResource SectionCaptionTextStyle}" />
                    <TextBlock x:Name="TodayLabel"
                               Margin="0,10,0,0"
                               FontSize="30"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}" />
                    <TextBlock Text="Copos validados no dia atual"
                               Style="{StaticResource BodyTextStyle}"
                               Margin="0,6,0,0" />
                </StackPanel>
            </Border>

            <Border Grid.Column="2"
                    Style="{StaticResource CardBorderStyle}"
                    Padding="18,16">
                <StackPanel>
                    <TextBlock Text="Sequência"
                               Style="{StaticResource SectionCaptionTextStyle}" />
                    <TextBlock x:Name="StreakLabel"
                               Margin="0,10,0,0"
                               FontSize="30"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource WarningBrush}" />
                    <TextBlock Text="Dias consecutivos batendo a meta"
                               Style="{StaticResource BodyTextStyle}"
                               Margin="0,6,0,0" />
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2"
                Style="{StaticResource CardBorderStyle}"
                Padding="18">
            <StackPanel>
                <TextBlock Text="Últimos 7 dias"
                           Style="{StaticResource SectionCaptionTextStyle}" />
                <TextBlock Text="Tendência recente da sua hidratação."
                           Style="{StaticResource BodyTextStyle}"
                           Margin="0,6,0,14" />
                <oxy:PlotView x:Name="WeeklyChart"
                              Height="300"
                              Background="Transparent"
                              BorderThickness="0" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
